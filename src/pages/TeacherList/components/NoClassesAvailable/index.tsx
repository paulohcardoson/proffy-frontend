import React from "react";

const NoClassesAvailable: React.FC = () => {
	return (
		<span className="text-text-complement font-semibold">
			Nenhuma aula disponível para esses filtros, tente encontrar outros.
		</span>
	);
};

export default NoClassesAvailable;
